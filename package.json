{
  "name": "staticflow-cms",
  "version": "0.1.0",
  "description": "Minimalista static site framework with clean routes, asset hashing and zero runtime",
  "type": "module",
  "bin": {
    "staticflow": "./bin/staticflow.js"
  },
  "scripts": {
    "dev": "node server/server.js",
    "dev:admin": "cd admin && ./node_modules/.bin/vite",
    "dev:full": "node server/server.js & cd admin && ./node_modules/.bin/vite --port 5173",
    "build:admin": "cd admin && ./node_modules/.bin/tsc -b && ./node_modules/.bin/vite build",
    "build:site": "node build.js build",
    "build:all": "npm run build:admin && npm run build:site",
    "start": "NODE_ENV=production node server/server.js",
    "server": "node server/server.js",
    "clean": "node build.js clean",
    "test": "node --test tests/**/*.test.js"
  },
  "dependencies": {
    "fastify": "^5.2.1",
    "@fastify/static": "^8.1.0",
    "@fastify/cors": "^11.0.0",
    "@fastify/cookie": "^11.0.2",
    "@fastify/multipart": "^9.0.3",
    "better-sqlite3": "^11.8.1",
    "dotenv": "^16.4.7",
    "uuid": "^11.1.0",
    "pino-pretty": "^13.0.0"
  },
  "devDependencies": {
    "html-minifier-terser": "^7.2.0",
    "terser": "^5.36.0",
    "clean-css": "^5.3.3",
    "sharp": "^0.33.5",
    "glob": "^10.4.5",
    "chokidar": "^3.6.0"
  }
}
